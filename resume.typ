// Dev preview. `typst watch resume.typ` renders the template's own example, so the
// live PDF always reflects what downstream gets. All content lives in the file below;
// all styling lives in lib.typ.
#include "template/main.typ"
