[package]
name = "mit-resume"
version = "0.1.0"
entrypoint = "lib.typ"
authors = ["Daniel Solano"]
license = "MIT"
description = "Two-page PhD resume following MIT CAPD's suggested formatting rules."

[template]
path = "template"
entrypoint = "main.typ"
